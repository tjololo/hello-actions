name: dummy test renovate

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      # renovate: datasource=github-tags depName=hello-actions/actions packageName=tjololo/hello-actions versioning=semver extractVersion=^action-composite-
      uses: tjololo/hello-actions/actions/composite@ac83d6fc2775ed63dd0cc5c1ad8ec2a607d4eee9 #regex v0.0.1
      with:
        gh_token: ${{ secrets.GITHUB_TOKEN }}