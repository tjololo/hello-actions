name: dummy test renovate

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      # renovate: datasource=github-tags depName=hello-actions/actions packageName=tjololo/hello-actions versioning=semver extractVersion=^action-composite-(?<version>.*)$
      uses: tjololo/hello-actions/actions/composite@0c74fcbf8c6ab7afd62e9239f212516b0b5bdeba #regex v0.0.2
      with:
        gh_token: ${{ secrets.GITHUB_TOKEN }}