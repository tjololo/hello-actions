server {
    listen       80;
    listen  [::]:80;
    server_name  localhost;

    #access_log  /var/log/nginx/host.access.log  main;
    #root /www/data

    location / {
        autoindex on;
        root /www/data;
    }

}